// FUNCTIONS
import { styled } from "src/lib/stitches.config"

export const Global = styled("div", {
  backgroundColor: "$bg-app",
  minHeight: "100%",
  p: "$base",
  display: "grid",
  placeItems: "center",
})
