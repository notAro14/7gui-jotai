import { styled } from "src/lib/stitches.config"

const Input = styled("input", {
  padding: "$sm",
  fontSize: "$base",
  fontFamily: "$main",
  borderRadius: "$md",
  backgroundColor: "$bg-ui",
  border: "none",
  color: "$hi-text",
  lineHeight: 1,
})

export default Input
